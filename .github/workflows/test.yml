on:
  push:
    branches:
  workflow_dispatch:

jobs:
  test:
    uses: cyberrangecz/ci-images/.github/workflows/test.yml
    with:
      packer_only: qemu
      packer_filename: kali.json
    secrets: inherit
